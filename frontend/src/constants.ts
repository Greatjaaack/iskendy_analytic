// Общие константы UI, бизнес-пороги и справочные списки/маппинги.
// Цель: убрать «магические» числа/строки/списки из компонентов и держать настройки
// в одном месте, чтобы их было легко найти и менять, не правя разметку.
import type { Period } from "./api";

// ─── Интервалы запросов (React Query) ───────────────────────────────────────

/** Автообновление данных на дашборде: каждые 60 сек дёргаем API заново. */
export const REFETCH_INTERVAL_MS = 60_000;
/** Сколько мс закэшированные данные считаются «свежими» (не перезапрашиваются). */
export const STALE_TIME_MS = 30_000;
/** Сколько раз повторять упавший запрос перед показом ошибки. */
export const QUERY_RETRY = 2;

// ─── Графики ─────────────────────────────────────────────────────────────────

/** Высота области графиков (px) — единая для всех графиков дашборда. */
export const CHART_HEIGHT = 280;

/** Тип отображения графика. Используется в «Выручка по дням» и «Выручка по часам». */
export type ChartKind = "area" | "line" | "step" | "bar";
export const CHART_TYPES: { key: ChartKind; label: string }[] = [
  { key: "area", label: "Область" },
  { key: "line", label: "Линия" },
  { key: "step", label: "Ступени" },
  { key: "bar", label: "Столбцы" },
];

// ─── Периоды дашборда ────────────────────────────────────────────────────────
export const PERIODS: { key: Period; label: string }[] = [
  { key: "day", label: "Сегодня" },
  { key: "week", label: "Неделя" },
  { key: "month", label: "Месяц" },
];

// ─── Дни недели (для фильтра «Выручка по дням») ──────────────────────────────
export const WEEKDAYS_ALL = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"];
export const WEEKDAYS_WORK = ["Пн", "Вт", "Ср", "Чт", "Пт"];   // будни
export const WEEKDAYS_WEEKEND = ["Сб", "Вс"];                  // выходные

// ─── Бизнес-пороги: маржа блюда (%) → цвет в таблице «Продажи блюд» ──────────
// Маржа = (выручка − себестоимость) / выручка. Чем выше — тем «здоровее» позиция.
/** ≥ 60% — хорошая маржа (зелёный). */
export const MARGIN_GOOD = 60;
/** ≥ 30% и < 60% — приемлемо (жёлтый). Ниже — красный. */
export const MARGIN_OK = 30;

// ─── Бизнес-пороги: food cost (%) на KPI-карточке (обратно марже) ───────────
/** ≤ 30% — отлично (зелёный). */
export const FOODCOST_GOOD = 30;
/** ≤ 40% — норма (жёлтый). Выше — красный. */
export const FOODCOST_WARN = 40;

// ─── Палитра ─────────────────────────────────────────────────────────────────
// Структурные цвета (фон/карточка/сетка/текст) берутся из CSS-переменных, которые
// переключаются темой (см. index.css и ThemeProvider). Акценты одинаковы в обеих темах.
export const COLORS = {
  bg: "var(--bg)",          // фон страницы / тёмный фон тултипов
  card: "var(--card)",      // фон карточек, графиков, таблиц
  grid: "var(--grid)",      // линии сетки графика и бордеры
  muted: "var(--muted)",    // приглушённый текст (подписи, второстепенное)
  text: "var(--text)",      // основной текст
  primary: "#6366f1",       // основной акцент (индиго)
  accent: "#22d3ee",        // вторичный акцент (циан): средний чек
  good: "#10b981",          // зелёный: хорошая маржа / низкий food cost
  warn: "#f59e0b",          // жёлтый: средние значения
  bad: "#ef4444",           // красный: плохо / ошибки
  indigoText: "#a5b4fc",    // светло-индиговый: доли, акцентные числа
} as const;

// ─── Погода: коды WMO (Open-Meteo) → эмодзи + описание ───────────────────────
// https://open-meteo.com/en/docs — weather_code. Группируем близкие коды.
export const WEATHER_CODES: Record<number, { icon: string; label: string }> = {
  0: { icon: "☀️", label: "Ясно" },
  1: { icon: "🌤️", label: "В основном ясно" },
  2: { icon: "⛅", label: "Переменная облачность" },
  3: { icon: "☁️", label: "Пасмурно" },
  45: { icon: "🌫️", label: "Туман" },
  48: { icon: "🌫️", label: "Изморозь" },
  51: { icon: "🌦️", label: "Морось" },
  53: { icon: "🌦️", label: "Морось" },
  55: { icon: "🌦️", label: "Морось" },
  61: { icon: "🌧️", label: "Дождь" },
  63: { icon: "🌧️", label: "Дождь" },
  65: { icon: "🌧️", label: "Сильный дождь" },
  71: { icon: "🌨️", label: "Снег" },
  73: { icon: "🌨️", label: "Снег" },
  75: { icon: "❄️", label: "Сильный снег" },
  80: { icon: "🌦️", label: "Ливень" },
  81: { icon: "🌧️", label: "Ливень" },
  82: { icon: "⛈️", label: "Сильный ливень" },
  95: { icon: "⛈️", label: "Гроза" },
  96: { icon: "⛈️", label: "Гроза с градом" },
  99: { icon: "⛈️", label: "Гроза с градом" },
};

/** Описание погодного кода (с фолбэком). */
export const weatherInfo = (code: number | null | undefined) =>
  code == null ? { icon: "", label: "" } : WEATHER_CODES[code] ?? { icon: "🌡️", label: "—" };
