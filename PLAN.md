# План развития Iskendy Analytics

Документ для согласования объёма работ перед реализацией. Описывает: что меняется,
архитектуру, модель данных, страницы и порядок сборки по фазам.

> **Статус (июнь 2026):** Фазы 0–3 завершены (логин iiko, каркас/роутинг, поставщики с
> контактами/каналами/экспортом, номенклатура ↔ ТТК с импортом и привязками блюд). Дашборд
> сильно расширен против исходного «без изменений»: вкладки Пульс/Операции/Меню, KPI с
> дельтами, разрезы по каналам/дейпартам/часам, состав и наполненность чека, меню-инжиниринг,
> галка «С доставкой», светлая/тёмная тема. **В работе:** Фазы 4–6 (накладные, остатки iiko,
> автозаказ). Актуальные детали — в `PROGRESS.md` и `README.md`.

## Спринт — кэш, автосинхронизация, UX-шапки (24 июня 2026) — ✅ сделано

8 пунктов от владельца, все закрыты (детали — в `PROGRESS.md`):
- (1) **Кэширование** живых чтений iikoweb — `backend/cache.py`, TTL `CACHE_TTL_SECONDS` (60 c), сброс при синке.
- (2) В подписи синка — **сперва время, затем дата** («14:05 · 23.06»).
- (3) **Автосинхронизация** с выбором интервала (5/15/30 мин / 1 час) — селектор в шапке + лёгкий `POST /api/sync?days=7`.
- (4) Галка **«С доставкой» без эмодзи**.
- (5) Погода на оси X «Выручки по дням» **разделена визуально** (иконка/температура/дельта в цвете).
- (6) В «Чеки и выручка по типу обслуживания» при выкл доставки строка **«Доставка» скрыта**; **эмодзи убраны**.
- (7) В «Выручке по дням» подпись дня — **день недели + число** (DD.MM).
- (8) Актуализированы README/CLAUDE/PROGRESS/PLAN/.env.example и память.

---

## Спринт — правки дашборда и костов (23–24 июня 2026) — ✅ сделано

Пакет из 21 правки от владельца — все закрыты и проверены вживую (docker + живой iiko).
Итоги (детали — в `PROGRESS.md`): погода только дневная (1); дейпарты на ширину (3) и без
подписи (8); ₽ убран из колонок (5); свод по дням — цвет групп + Пн→Вс без баров (4,6);
неделя с понедельника (7); детект доставки = категория «Доставка» ИЛИ маркер `_д` (2,16);
колонки План/Факт с/с (19); удаление поставщиков (20); канонизация+слияние имён
поставщиков 21→13 (21); состав чека — фикс долей (12,13); продажи по часам — drill
категория→блюда (10,11); матрица сочетаемости `MenuBasket`/`/api/dishes/basket` (14);
сверка себестоимостей по seed (17); категория iiko **«Меню» → «Допы и соусы»**
(`CATEGORY_DISPLAY`, гарниры/соусы — Фри/Дип/Кетчуп; не баг, дефолтная корзина iiko) (9,15);
документация+память (18). Ниже — исходная постановка спринта (для истории).

### Исходная постановка (для истории)

Запрошенный пользователем пакет правок. Сгруппирован по областям; в скобках — исходные
номера пунктов. Пометка ⚠️ = требует уточнения/разбора перед реализацией.

### A. Дашборд — отображение и данные

- **(1) Погода — только дневная.** Не показывать ночную температуру/погоду, оставить
  дневной показатель (`weather.py` + ось X в `RevenueChart`).
- **(2) «Чеки и выручка по типу обслуживания» (`ChecksDistribution`) — реагирует на галку.**
  Сейчас виджет всегда показывает все статусы и НЕ зависит от «С доставкой». Изменить: при
  `withDelivery=false` исключать доставку из обоих рядов (доля чеков + доля выручки).
  Затрагивает `check-distribution` и `revenue/by-channel`, фронт `ChecksDistribution`.
  Обновить описание в `CLAUDE.md`/`README.md` (там сейчас явно сказано «не зависит от галки»).
- **(3) «Выручка по дейпартам» (`DaypartBreakdown`) — на всю ширину.** Перевести в `.dash-full`,
  убрать из 2-колоночной сетки.
- **(4) ⚠️ «Свод по дням недели» — непонятные «шкалы заполненности».** Это инлайн-бары в колонке
  «Ср. за день». Решение: убрать их (или заменить понятной подписью). Уточнить у пользователя,
  если оставлять — добавить пояснение, что бар = доля от максимума.
- **(5) Убрать знак «₽» в колонках таблиц.** Денежные значения без символа валюты (формат в
  `format.ts`; таблицы `WeekdaySummary`, `DaypartBreakdown`, `DishTable` и пр.).
- **(6) «Свод по дням недели» — цвет дней + фикс-сортировка.** Красить строки по группам дня
  недели (как `WEEKDAY_GROUPS` в `RevenueChart`) и всегда сортировать Пн→Вс (убрать/переопределить
  пользовательскую сортировку либо сделать её опциональной поверх дефолта Пн→Вс).
- **(8) Убрать подпись «Когда зарабатывает день — окна по времени»** из `DaypartBreakdown`.
- **(9)+(15) ⚠️ Категория/группа «меню» — похоже на ошибку.** В коде такой константы нет —
  значит приходит из данных iiko (пустая/служебная категория блюда). Разобраться, где всплывает
  (`CheckComposition`, `MenuEngineering`, группировки `/api/dishes`), и либо корректно
  переименовать/скрыть, либо отнести к `NON_PRODUCT_CATEGORIES`.
- **(10) «Продажи по часам» (`HourlyBreakdown`), режим «Доли» — читаемость мелких долей.**
  Доминирующие категории (Дюрюм) забивают мелкие (напитки). Варианты: лог-шкала/отдельная
  легенда/выделение мелких. Подобрать визуализацию.
- **(11) «Продажи по часам» — раскрытие категории → проваливание в блюда.** Drill-down: клик по
  категории в стеке/таблице раскрывает её блюда (данные уже есть через `group=dish`).
- **(12) «Состав чека» (`CheckComposition`) — баг «0.000001%» и визуализация долей.** Исправить
  отображение микродолей (округление/порог «<0.1%») и переосмыслить визуализацию (масштабирование,
  группировка хвоста в «Прочее»).
- **(13) ⚠️ «Состав чека»: «Чеков в периоде: 160» vs 164 в KPI.** Расхождение числа чеков между
  `check-composition` и `KpiCards`. Разобраться (разные источники: OLAP-уникальные заказы vs
  `REV_GROSS`-чеки; возможно фильтр модификаторов/доставки). Свести к одному определению чека.
- **(14) Новый виджет — матрица сочетаемости блюд (market basket).** Какие позиции чаще берут
  вместе. OLAP по `OrderNum`: со-встречаемость пар категорий/блюд → матрица/heatmap или топ-пары.
  Новый эндпоинт + компонент.

### B. Детект доставки

- **(2)+(16) Расширить правило «что считается доставкой».** Сейчас доставка = строго меню-категория
  «Доставка». Добавить: **всё с префиксом `_д` в наименовании — доставка.** Уточнить, дополняет ли
  это категорию или заменяет; провести через хелперы `_delivery_buckets`/`_exclude_delivery` и
  пер-позиционный отсев в `dishes.py`. Синхронизировать с галкой во всех виджетах (см. п. 2).

### C. Себестоимость и поставщики

- **(17) Сверка себестоимостей всех позиций** с Google-таблицей
  `https://docs.google.com/spreadsheets/d/1FSAjAXzPXtPbo9n8J4nNIR6YIKSG9rm-/` (лист gid=1728521734).
  Скачать/сохранить данные, пройтись по `cost_full` каждой ТТК, найти расхождения, починить.
- **(19) Колонки план/факт с/с.** В таблицу блюд (`DishTable`): **планируемый с/с в рублях** и
  **в косте (%)**, **фактический** и **дельта** план↔факт. Фактический пока не считаем — колонка
  есть, значение «—»/null; позже подключим расчёт по накладным/расходу.
- **(20) Удаление поставщиков.** `DELETE /api/suppliers/{id}` + кнопка на карточке/в списке
  (каскад: контакты, файлы, цены; предупреждение).
- **(21) Нормализация имён поставщиков** в листе «Матрица поставщиков» Google-таблицы: привести
  к единому канону (WB = wildberries.ru; METRO = Мэтро = «METRO Chef …»). Канонизация при импорте
  (`importers/ttk_matrix.py`) или словарь алиасов.

### D. Пресеты периода

- **(7) Пресеты «неделя»/«месяц» — календарные.** «Неделя» = с понедельника текущей недели (не
  последние 7 дней); «месяц» = с 1-го числа текущего месяца (уже MTD — проверить). Правка
  `period_range` в `utils.py`; синхронизировать `prev_period_range` для KPI-дельт.

### E. Документация и память

- **(18) Зафиксировать все изменения** в `README.md`, `CLAUDE.md`, `PROGRESS.md` и в памяти
  (после реализации каждого блока).

### Решения по этому спринту (уточнено)

- (4) Инлайн-бары в «Своде по дням недели» — **убрать полностью.**
- (16) Префикс `_д` — **дополняет** меню-категорию «Доставка» (доставка = категория «Доставка»
  ИЛИ имя начинается с `_д`).
- (19) «Кост» = **с/с ÷ цена реализации** (пример: с/с 25 ₽, цена 100 ₽ → кост 25%).

---

## Зафиксированные решения

| Вопрос | Решение |
|---|---|
| Запись в iiko | **НЕ пишем.** Косты, ТТК, поставщиков, заказы ведём в собственной БД. iiko только на чтение. |
| Остатки для автозаказа | **Из склада iiko** (новый read-эндпоинт остатков). |
| Форматы накладных | **Excel (.xlsx/.xls)** + **PDF-сканы** (не фото). Сканы → через распознавание. |
| Порядок сборки | Сначала фундамент-справочники, затем зависимые модули (см. Фазы). |

> Важная корректировка исходной задачи: пункты «заносишь в Айку веб» и «ТТК в Айке
> внести» меняются на «ведём у себя». Всё, что раньше шло «в iiko», теперь хранится
> в нашей системе как источник правды по костам.

## Предусловие (Фаза 0) — починить логин в iiko

Сейчас Playwright-логин падает (`Page.click: Timeout` на кнопке «Войти») — данные не
тянутся вообще. Без рабочего логина не будет ни продаж, ни остатков. Чиним первым делом:
поправить селекторы формы по реальной вёрстке, прогнать в видимом режиме до успеха.

## Архитектура и стек (дополнения к текущему)

- **Backend (FastAPI)** — добавляем модули: загрузка/парсинг файлов, поставщики, ТТК,
  заявки. Хранилище файлов — на диске в volume (`/data/files`), путь пишем в БД.
- **Парсинг накладных:**
  - Excel → `openpyxl`/`pandas` (надёжно, правила по шаблону поставщика).
  - PDF-скан → распознавание. Рекомендую vision-модель (Claude API) — даёт структурный
    JSON по таблице накладной с высокой точностью. Альтернатива — локальный Tesseract
    (бесплатно, но точность ниже, требует больше ручной правки). ← нужно подтвердить.
- **БД** — пока SQLite (как сейчас). При росте объёма легко перенести на Postgres.
- **Frontend (React)** — появляется несколько страниц → добавляем роутинг
  (`react-router-dom`) и боковое меню. Сейчас одностраничный дашборд.
- **iiko (чтение):** продажи (есть) + **остатки склада** (новый эндпоинт, реверс).

## Модель данных (новые таблицы)

```
suppliers              поставщики: name, contact_person, phone, address,
                       min_delivery, comment
supplier_files         файлы поставщика: supplier_id, path, type(накладная/прайс), uploaded_at
products               зеркало номенклатуры iiko: iiko_id(uuid), name, category, unit
supplier_products      товар у поставщика: supplier_id, supplier_item_name,
                       product_id(=привязка к номенклатуре, может быть пустой),
                       pack/фасовка, current_price
price_history          история цен: supplier_product_id, price, date, invoice_id
invoices               накладные: supplier_id, number, date, total, file_id, status
invoice_lines          строки накладной: invoice_id, supplier_item_name,
                       matched_product_id, qty, unit, price, sum
ttk                    тех-тех карты: name, dish_id(iiko), выход
ttk_ingredients        состав ТТК: ttk_id, product_id, норма закладки, unit
stock_levels           остатки из iiko: product_id, qty, unit, snapshot_date
orders                 заявки: supplier_id, date_for, status, total
order_lines            строки заявки: order_id, product_id, qty, unit, price, sum
```

**Связующая логика (расчёт расхода → заказа):**
`продажи блюд (из iiko) × ttk_ingredients` = расход продуктов →
сверка с `stock_levels` → потребность → группировка по поставщикам = `orders`.

---

## Страницы (детально)

### Дашборд (есть)
Выручка, блюда, KPI. Без изменений (позже добавим виджет костов).

### 1. Накладные (приёмки и косты)
- Загрузка файла (Excel/PDF-скан), выбор поставщика.
- Распознавание: шапка (дата, №, поставщик, сумма) + позиции (наименование, кол-во,
  ед.изм, цена, сумма).
- Автопривязка позиций к номенклатуре iiko по сохранённым соответствиям (стр. 5);
  непривязанные правим вручную.
- Экран предпросмотра → подтверждение пользователем.
- Сохранение: строки костов + история цен; оригинал файла → в карточку поставщика.
- **Результат:** актуальные закупочные цены, косты всегда видны.

### 2. Поставщики (справочник)
- Список/карточки: название, контакт, телефон, мин.поставка.
- Карточка поставщика:
  - Реквизиты: название, контактное лицо, телефон, адрес, мин.поставка, комментарий.
  - Товары поставщика + цена по каждому (текущая + история, график).
  - История заказов и накладных с прикреплёнными файлами (Excel/PDF).
- **Результат:** один экран — всё о поставщике.

### 3. Новый / редактирование поставщика
- Форма реквизитов.
- Загрузка файлов (прайс/накладные) → авто-парсинг предзаполняет товары и цены.
- **Результат:** завёл контрагента + закинул прайс → товары подтянулись сами.

### 4. Автозаказ (ночное формирование заявок)
- Ночной cron: продажи × ТТК → расход → сверка с остатками iiko → потребность.
- Группировка заявок по поставщикам (позиции, кол-во, цена из последней накладной).
- Экран заявок: правка количеств, учёт мин.поставки.
- Экспорт/отправка поставщику (Excel/PDF).
- **Результат:** утром готовы заявки на день вперёд по реальному расходу.

### 5. Номенклатура ↔ ТТК
- Список номенклатуры iiko + справочник ТТК.
- Ведение ТТК (состав: ингредиент + норма).
- Привязки: наименование поставщика → позиция iiko → ТТК, где используется.
- **Результат:** единый словарь, связывающий накладные, расход и косты.

---

## Фазы реализации (порядок)

- **Фаза 0 — Починка логина iiko** (предусловие, без него ничего не тянется).
- **Фаза 1 — Каркас:** роутинг + меню на фронте; базовые таблицы; хранилище файлов.
- **Фаза 2 — Поставщики (стр. 2, 3):** справочник, карточки, загрузка файлов.
- **Фаза 3 — Номенклатура ↔ ТТК (стр. 5):** импорт номенклатуры, ведение ТТК, привязки.
- **Фаза 4 — Накладные (стр. 1):** парсинг Excel → затем PDF-сканы; косты, история цен.
- **Фаза 5 — Остатки iiko:** реверс эндпоинта остатков, синк в `stock_levels`.
- **Фаза 6 — Автозаказ (стр. 4):** расчёт расхода, ночной cron, экран и экспорт заявок.

## Открытые вопросы (нужно для старта)

1. **Склад в iiko:** реально ли в точке ведётся складской учёт и есть остатки?
   (Если нет — автозаказ строить не на чем, перейдём на расчётные остатки.)
2. **ТТК:** в каком виде дашь карты (Excel/PDF) — чтобы спланировать импорт.
3. **Распознавание сканов:** ок ли использовать Claude API (vision, платно, нужен ключ)
   для PDF-сканов, или ставим бесплатный Tesseract (точность ниже)?
4. **Фасовки/единицы:** как считать пересчёт (кг ↔ шт ↔ коробка)? Нужен справочник
   фасовок у каждого поставщика, иначе кол-во в заявке будет неточным.
5. **Хранилище файлов:** локально в volume контейнера — устраивает? (или нужно облако).
